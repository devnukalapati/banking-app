# Supabase PostgreSQL connection
DATABASE_URL=jdbc:postgresql://db.eihluoklnriasdnovrsk.supabase.co:5432/postgres
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your-supabase-database-password

# AES-256 encryption key — generate with: openssl rand -base64 32
ENCRYPTION_SECRET_KEY=your-base64-encoded-32-byte-key

# React dev server origin
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Optional: override default port 8080
# SERVER_PORT=8080
